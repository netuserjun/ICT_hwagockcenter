	프로세스 process
		동작하는 프로그램.

		부모 프로세스는 folk 함수로 자식 프로세스를 동작(생성)시킨다.
		이때 자식 프로세스는 PID와 메모리를 할당받는다.

		자식 프로세스가 동작(run)할 때 상태가 변하기 위해서 interrupt를 걸어준다.

같은 레벨의 자식 프로세스가 여러 개일 때 하나를 선택하려면 세마포어 사용

exit를 통해 부모프로세스에게 PID와 메모리를 반환하고 소멸된다.
고아프로세스 : 부모프로세스가 죽은 자식 프로세스. 현재는 존재하지 않음.
조상프로세스 : 모든 프로세스의 가장 꼭대기에 있는 조상.

좀비 프로세스 : 실행이 종료되었지만 아직 삭제되지 않은 프로세스

프로세서 processor
		cpu의 연산처리를 하기 위한 단위


포그라운드와 백그라운드
	백그라운드 작업은 &를 붙여서 명령.
	백그라운드 작업을 보려면 jobs 입력

	fg %작업번호 : 백을 포로 가져오는 명령

백그라운드 명령을 했을 때 포그라운드 화면은 새로운 프로세스다.
	

압축 
	compress 파일명
	uncompress 파일명.Z  : 압축해제
		
		zip 파일명.zip 파일명  : 압축시 압축파일명 지정해야함.
		unzip aaa.zip : 압축해제

gzip -1~9 파일명 : 숫자가 클 수록 느리지만 압축률은 좋다.
gunzip 파일명.gz : 압축해제

bzip2 파일명 : 단일파일 압축에서 좋다.
bzip2 -d 파일명 : 압축해제


아카이브
	tar cvf 아카이브명 파일명 파일명 : 파일 2개를 하나로 묶는거
	tar xvf 아카이브명 : 묶은거 다시 푸는거.

	
----

osi 7
	
	물리계층
PDU(프로토콜 데이터 유닛) : BIT 	
		
HUB : 전기 신호를 흘려주기 위한 장비.  충돌을 고려하지 않음.
스위칭 허브 : 대표적인 스위치. 속도가 보장된다.
더미허브 : tap 장비를 대신해서 통신정보를 미러링하기 위해 사용. 모니터링
리피터, 케이블

데이터링크계층
PDU : frame 	 
	
	스위치 : 충돌을 고려하고 만든 장비
		MAC Table : 포트 별로 연결되는 단말기의 고유 mac 정보를 가진다. 
	LLC : logical link control 흐름제어
	MAC : 랜카드의 하드웨어 주소 정보. 고유한 주소. 앞 3자리가 회사정보(OUI).
	브리지

2.5 계층(비공식)
	IP > MAC : ARP
MAC > IP : RARP

네트워크계층
PDU : 패킷	
프로토콜 : ICMP IP IPSEC IGMP
라우터 : 최적 경로 탐색 

전송계층
	PDU : segment
	port : PID 별로 통신을 맵핑하기 위해 사용하는 단위.
		system port 0-1023, user 1024-49151, Dynamic & private (-65535) 
	UDP TCP SCTP DCCP

세션계층
	PDU : data
	흐름제어
	QoS : quality of service 특정 서비스를 위해 트래픽을 제어하는것
	
	표현계층
		PDU : data
		인코딩, 압축, 코덱, 암호화(SSL,TLS) 
		전송계층부터의 암호화 담당
		JPEG, MPEG

	응용프로그램계층
		PDU : data
		ftp, ssh, telnet, DNS, web, https, SMTP, oracle, mysql/mariadb, mssql
